Use Skill: asc-director

You are working in:
`/Users/bytedance/repo/android/personal/cmd-android-reborn`

Task ID:
A07_libterm_runtime

Read these files first:
- `/Users/bytedance/repo/android/personal/cmd-android-reborn/docs/.asc_task/libterm_master/master.md`
- `/Users/bytedance/repo/android/personal/cmd-android-reborn/docs/.asc_task/libterm_master/tasks/A07_libterm_runtime.md`
- Existing A01-A06 docs and code, if present.

Goal:
Implement the Android-facing runtime/facade layer that wires core and backends into an easy-to-use entry point.

Scope:
- Provide a simple way for App code to create a default LibTerm manager/facade.
- Register USER/ROOT providers from libsu backend.
- Register Shizuku provider if available or placeholder if not implemented.
- Build backend factory.
- Pass explicit open failures through to the caller.
- Keep core state machine logic out of runtime.

Non-goals:
- No UI.
- No automatic fallback.
- No persistence.
- No command history.
- No backend-specific behavior inside core.

Skills:
- First invoke `asc-director`.
- Use `Karpathy Guidelines`.

Testing requirements:
- Use fake providers/backends to test wiring.
- Cover:
  - default runtime creates manager
  - USER provider registered
  - ROOT provider registered
  - Shizuku provider registered or placeholder behavior is explicit
  - open failure propagates
  - no fallback occurs
- Do not test libsu/Shizuku internals here.

Acceptance criteria:
- App integration path is simple and documented.
- Runtime depends on core and backends, not the other way around.
- Errors are not swallowed.
- No business logic duplicated from `TerminalManager`.
- Provide changed files and suggested verification commands.
- Do not run full build unless user explicitly approves.